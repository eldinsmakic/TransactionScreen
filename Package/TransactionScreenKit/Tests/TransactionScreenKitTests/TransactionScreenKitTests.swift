import XCTest
@testable import TransactionScreenKit

final class TransactionScreenKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TransactionScreenKit().text, "Hello, World!")
    }
}
